
CMAKE_MINIMUM_REQUIRED(VERSION 2.6 FATAL_ERROR)
CMAKE_POLICY(VERSION 2.6)
FIND_PACKAGE(Torch REQUIRED)

SET(src init.c)
SET(luasrc init.lua)

ADD_TORCH_PACKAGE(xml "${src}" "${luasrc}" "Data structures")
TARGET_LINK_LIBRARIES(xml luaT TH)
